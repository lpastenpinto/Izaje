
@{
    ViewBag.Title = "Nuevo Modelo Equipo";
}

<div class="container">
    <div class="panel-body">
        <div class="page-header">
            <h2 class="text-center">Nuevo Modelo Equipo</h2>
        </div>
    </div>
    <form action="@Url.Action("Guardar_Modelo_Equipo", "Admin")" class="form-horizontal" role="form" method="post">
        <div class="form-group">
            <label class="col-sm-2 control-label">Modelo</label>
            <div class="col-sm-10">
                <input type="text" class="form-control" name="modelo" pattern="[a-zA-Z0-9áéíóú ]+" placeholder="Nombre Modelo Equipo.." required>
            </div>
        </div>

        <div class="form-group">
            <label class="col-sm-2 control-label">Marca</label>
            <div class="col-sm-10">
                <select class="form-control" name="marca" required>
                    @foreach (WebIzaje.Models.Marca_Equipo x in ViewBag.marca)
                    {
                        <option>@x.marca</option>
                    }
                </select>
            </div>
        </div>

        <div class="form-group">
            <label class="col-sm-2 control-label">Tipo Equipo</label>
            <div class="col-sm-10">
                <select class="form-control" name="tipo" required>
                    @foreach (WebIzaje.Models.tipoEquipo x in ViewBag.tipo)
                    {
                        <option>@x.tipo</option>
                    }
                </select>
            </div>
        </div>


        <div class="modal-footer">
            <a type="button" href="@Url.Action("Modelo_Equipos","Admin")" class="btn btn-default btn-lg"><i class="glyphicon glyphicon-log-out"></i>&nbsp;Cancelar</a>
            <button type="submit" class="btn btn-primary btn-lg"><i class="glyphicon glyphicon-floppy-disk"></i>&nbsp;Guardar</button>
        </div>
    </form>
</div>





